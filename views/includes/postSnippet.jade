div.post
  h1
    a(href=post.url)
      = post.title
  span.date Posted on #{post.form_date} in #{post.category}
  div.post.content
    != post.preview
  div.tags
    - each tag in post.tags
      a(href=tagUrl(tag))
        span.label.label-info #{tag}
